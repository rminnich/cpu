syntax = "proto3";

option go_package = ".:cpu";

package protobuf;

service StreamService {
  rpc Stdout (Request) returns (stream Response) {}
}

message Request {
  int32 id = 1;
  string stdin = 2;
}

message Response {
  int32 fd = 1;
  string result = 2;
}
